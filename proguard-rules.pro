# Add project specific ProGuard rules here.
# For OkHttp, if you enable shrinking:
# -keepattributes Signature
# -keepattributes *Annotation*
# -keep class okhttp3.** { *; }
# -keep interface okhttp3.** { *; }
# -dontwarn okhttp3.**
# -dontwarn okio.**
